This is a duplicate of https://www.npmjs.com/package/imap v.0.8.19 with the following updates:

- Added type definitions for TypeScript support
- Updated buffer usage to use `Buffer.from` instead of `new Buffer`
